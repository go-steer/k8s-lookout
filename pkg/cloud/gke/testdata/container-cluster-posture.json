{
  "//": "Recorded-shape fixture: container.googleapis.com/v1 projects.locations.clusters.get response for a cluster with the posture gaps `audit cluster` and `audit upgrades` report (fields the clusterconfig read consumes, current controlPlaneEndpointsConfig surface). Subscribed to no release channel, with no maintenance policy and no notificationConfig — the blocks whose ABSENCE is the finding. Authored from the GKE Cluster resource API reference — not captured from a live project (§13).",
  "name": "exposed",
  "location": "us-central1",
  "endpoint": "203.0.113.10",
  "currentMasterVersion": "1.29.7-gke.1104000",
  "controlPlaneEndpointsConfig": {
    "ipEndpointsConfig": {
      "enabled": true,
      "enablePublicEndpoint": true,
      "publicEndpoint": "203.0.113.10",
      "privateEndpoint": "10.0.0.2",
      "authorizedNetworksConfig": {
        "enabled": true,
        "gcpPublicCidrsAccessEnabled": true,
        "cidrBlocks": [
          { "displayName": "office", "cidrBlock": "198.51.100.0/24" },
          { "displayName": "everywhere", "cidrBlock": "0.0.0.0/0" }
        ]
      }
    }
  },
  "nodePools": [
    {
      "name": "default-pool",
      "status": "RUNNING",
      "version": "1.29.7-gke.1104000",
      "management": { "autoUpgrade": true, "autoRepair": true },
      "config": {
        "machineType": "e2-standard-4",
        "imageType": "COS_CONTAINERD",
        "metadata": { "disable-legacy-endpoints": "true" },
        "workloadMetadataConfig": { "mode": "GKE_METADATA" }
      }
    },
    {
      "name": "legacy-pool",
      "status": "RUNNING",
      "version": "1.27.11-gke.1062000",
      "management": { "autoUpgrade": false, "autoRepair": false },
      "config": {
        "machineType": "n1-standard-8",
        "imageType": "COS",
        "metadata": { "disable-legacy-endpoints": "false" },
        "workloadMetadataConfig": { "mode": "GCE_METADATA" }
      }
    },
    {
      "name": "unconfigured-pool",
      "status": "RUNNING",
      "config": {
        "machineType": "e2-standard-2"
      }
    }
  ]
}
